import React from "react";

const SearchBar = () => {
  return (
    <div className="searchBar">
      <input type="text" placeholder="Not Ara..." />
      <button>Ara</button>
    </div>
  );
};

export default SearchBar;
